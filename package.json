{
  "name": "tesla-family-center",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 execute tesla-family-center-db --local --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.123.0"
  }
}
